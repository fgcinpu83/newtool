# Provider Lamp & Balance Data Flow

## Last Updated: 2026-01-09

## Overview
This document describes the complete data flow from browser sniffer to UI provider lamps.

---

## 🔄 Complete Data Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              BROWSER (Sniffer)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│  AFB88_sniffer.js / BTI_sniffer.js                                          │
│                                                                              │
│  1. DOM Watcher detects balance from #txtBalance element                     │
│  2. Network Interceptor captures odds from XHR/Fetch                         │
│  3. Heartbeat sent every 10 seconds with balance                             │
│                                                                              │
│  transport.send('balance', '6.36')                                           │
│  transport.send('heartbeat', { provider, timestamp })                        │
│  transport.send('odds_batch', [...]) // Every 3 seconds from DOM scraper     │
│                                                                              │
│  ↓ Socket.IO emit('endpoint_captured', { account, type, provider, data })    │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         BACKEND - Gateway Layer                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  gateway.module.ts - AppGateway                                              │
│                                                                              │
│  @SubscribeMessage('endpoint_captured')                                      │
│  handleEndpointCaptured(data) {                                              │
│      // Log to wire_debug.log                                                │
│      this.commandEvents.emit('endpoint_captured', data);  ──────────────────┐│
│  }                                                                          ││
└─────────────────────────────────────────────────────────────────────────────┘│
                                                                               │
           ┌───────────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    BACKEND - Service Listeners (Parallel)                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────┐     ┌─────────────────────────────────────────┐ │
│  │   WorkerService         │     │    ProviderGuardianService              │ │
│  │   worker.service.ts     │     │    provider-guardian.service.ts         │ │
│  ├─────────────────────────┤     ├─────────────────────────────────────────┤ │
│  │ Listens for:            │     │ Listens for:                            │ │
│  │ - endpoint_captured     │     │ - endpoint_captured                     │ │
│  │                         │     │ - provider:presence                     │ │
│  │ Processes:              │     │                                         │ │
│  │ - balance → Updates     │     │ Processes:                              │ │
│  │   balance[account]      │     │ - heartbeat → lastHeartbeatTime         │ │
│  │ - odds_batch → Forward  │     │ - balance → lastBalanceTime,            │ │
│  │   to DiscoveryService   │     │   state = CONNECTED                     │ │
│  │                         │     │ - odds → lastMatchTime,                 │ │
│  │ Broadcasts:             │     │   state = HEALTHY                       │ │
│  │ - system_status event   │     │                                         │ │
│  └───────────┬─────────────┘     │ Broadcasts:                              │ │
│              │                   │ - guardian:status event                  │ │
│              │                   └─────────────────┬───────────────────────┘ │
│              │                                     │                         │
└──────────────┼─────────────────────────────────────┼─────────────────────────┘
               │                                     │
               ▼                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND - WebSocket Listeners                       │
├─────────────────────────────────────────────────────────────────────────────┤
│  page.tsx                                                                    │
│                                                                              │
│  socket.on('system_status', (data) => {                                     │
│      // Updates balanceA, balanceB, providerStatus                          │
│      setSystemStatus(data);                                                 │
│  });                                                                        │
│                                                                              │
│  socket.on('guardian:status', (data) => {                                   │
│      // Updates guardian state for each provider                            │
│      setGuardianStatus(data);                                               │
│  });                                                                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND - UI Rendering                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Balance Display:                                                            │
│  ┌─────────────────────────────────────┐                                     │
│  │ BALANCE: {systemStatus.balanceA}    │  ← From system_status event        │
│  └─────────────────────────────────────┘                                     │
│                                                                              │
│  Provider Lamp Colors (Priority Order):                                      │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                                                                          │ │
│  │  IF guardianEntry exists:                                                │ │
│  │    🟢 GREEN  = HEALTHY or CONNECTED (receiving data)                     │ │
│  │    🟡 YELLOW = HEARTBEAT_ONLY (degraded, was healthy, now no match data) │ │
│  │    🟠 ORANGE = RECOVERING (auto-recovery in progress)                    │ │
│  │    🔴 RED    = DEAD or NO_DATA (no communication > 90s)                  │ │
│  │                                                                          │ │
│  │  ELSE fallback to WorkerService state:                                   │ │
│  │    🟢 GREEN  = LIVE                                                      │ │
│  │    🟡 YELLOW = SESSION_BOUND                                             │ │
│  │    ⚫ GRAY   = INACTIVE                                                  │ │
│  │                                                                          │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🛡️ Guardian State Machine

```
                              ┌───────────┐
                              │  NO_DATA  │  ← Initial state
                              └─────┬─────┘
                                    │
           ┌────────────────────────┴────────────────────────┐
           │ Receives heartbeat                              │ Receives balance
           ▼                                                 ▼
    ┌─────────────┐                                   ┌─────────────┐
    │  CONNECTED  │  ← GREEN (balance/heartbeat only) │  CONNECTED  │
    └──────┬──────┘                                   └──────┬──────┘
           │                                                 │
           │ Receives odds_batch/match_batch                 │
           ▼                                                 ▼
    ┌─────────────┐                                   ┌─────────────┐
    │   HEALTHY   │  ← GREEN (full data flow)         │   HEALTHY   │
    └──────┬──────┘                                   └──────┬──────┘
           │                                                 │
           │ No match data > 3 minutes                       │
           ▼                                                 │
    ┌────────────────┐                                       │
    │ HEARTBEAT_ONLY │  ← YELLOW (degraded)                  │
    └───────┬────────┘                                       │
            │                                                │
            │ Triggers recovery                              │
            ▼                                                │
    ┌─────────────┐                                         │
    │  RECOVERING │  ← ORANGE                               │
    └──────┬──────┘                                         │
           │                                                │
           │ No heartbeat > 90s                             │
           ▼                                                │
    ┌─────────────┐                                         │
    │    DEAD     │  ← RED                                  │
    └─────────────┘                                         │
           ▲                                                │
           └────────────────────────────────────────────────┘
                         Recovery fails
```

---

## 🔧 Critical Fix Applied (2026-01-09)

### Issue Found:
`WorkerService.handleEndpointCaptured()` method existed but was **NEVER CALLED** because there was no event listener connecting it to `AppGateway.commandEvents`.

### Fix:
Added listener in `WorkerService.onModuleInit()`:
```typescript
this.gateway.commandEvents.on('endpoint_captured', (data) => {
    this.handleEndpointCaptured(data);
});
```

### Result:
- Balance data now flows from sniffer → backend → UI ✅
- Provider registration works correctly ✅
- Lamp colors update in real-time ✅

---

## 📊 Monitoring Logs

| Log Pattern | Location | Meaning |
|------------|----------|---------|
| `[INGEST-GATEWAY]` | Gateway | Raw packet received from sniffer |
| `[BALANCE-UPDATE]` | WorkerService | Balance successfully parsed and stored |
| `[GUARDIAN]` | GuardianService | Provider state changed |
| `[DISCOVERY-ENTRY]` | DiscoveryService | New match event registered |
| `[STATUS-CHANGED]` | HealthMonitor | Overall system health changed |

